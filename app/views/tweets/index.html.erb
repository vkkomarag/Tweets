
<table>
<tr>
<th>Username</th>
<th>Message</th>
<th>Latitude</th>
<th>Longitude</th>
<th>Show Map</th></tr>
<% @tweets.each do |t| %>
<tr>
	<td>
		<%= t.user_name %>
	</td>
	<td><%= t.message %></td>
	<td><%= t.latitude %></td>
	<td><%= t.logitude %></td> 
	<td><%= link_to "Show Map", tweets_show_path(t) %></td>
</tr>
<% end %>
</table>
<%= will_paginate @tweets %>

